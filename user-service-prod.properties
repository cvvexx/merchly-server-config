spring.datasource.url=jdbc:postgresql://${USERS_DB_HOST:users_db}:${USERS_DB_PORT:5432}/${USERS_DB_NAME}
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.initialization-fail-timeout=0
spring.security.oauth2.resourceserver.jwt.issuer-uri=${KEYCLOAK_ISSUER_URI:http://keycloak:8090/realms/merchly}
keycloak.server-url=${KEYCLOAK_SERVER_URL:http://keycloak:8090}
