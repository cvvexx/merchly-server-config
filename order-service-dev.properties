spring.datasource.url=jdbc:postgresql://${ORDERS_DB_HOST:localhost}:${ORDERS_DB_PORT:5436}/${ORDERS_DB_NAME}
spring.security.oauth2.resourceserver.jwt.issuer-uri=${KEYCLOAK_ISSUER_URI:http://localhost:8090/realms/merchly}
spring.security.oauth2.client.provider.keycloak.token-uri=${KEYCLOAK_TOKEN_URI:http://localhost:8090/realms/merchly/protocol/openid-connect/token}

spring.restclient.uri.product_service=${PRODUCT_SERVICE_URI:http://localhost:8081}
spring.restclient.uri.user_service=${USERS_SERVICE_URI:http://localhost:8082}
